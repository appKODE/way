digraph NavService04 {
  package = "ru.kode.way.nav04"

  app [type = flow]
  permissions [type = flow]
  profile [type = flow]

  app -> permissions
  permissions -> intro
  permissions -> request

  app -> profile -> main
}
